import React, { Component } from 'react'
import { render } from 'react-dom'
import styles from './less/main.less'

render(
  <div>
    <h1>Renderd with ReactJS</h1>
  </div>,
  document.getElementById('app-container')
)
